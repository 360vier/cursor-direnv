---
"cursor-direnv": minor
---

Ship the initial `cursor-direnv` CLI with local/global install and uninstall commands for the Cursor direnv hook.

Add test coverage for hook rewriting and hooks.json merge behavior, plus GitHub workflows for CI and automated releases.
