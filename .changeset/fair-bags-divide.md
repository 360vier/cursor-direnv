---
"cursor-direnv": patch
---

Remove the local Cursor rules file (`.cursor/rules/direnv-hook.mdc`) when running local uninstall so install and uninstall clean up the same files.

Add integration coverage and documentation updates for the local uninstall cleanup behavior.
